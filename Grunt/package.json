{
  "name": "grunt",
  "version": "1.0.0",
  "description": "testing/using Grunt task runner !",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "grunt-cli": "^1.2.0"
  },
  "devDependencies": {
    "grunt-contrib-uglify": "^2.0.0"
  }
}
